# TM-REQ-001 Rev A — Python Dependencies for NEC Antenna Modeling System
# Install: pip install -r requirements.txt
# Python >= 3.10 required

# Core numerical
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0

# Visualization
matplotlib>=3.7.0

# Web API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
python-multipart>=0.0.6
aiofiles>=23.2.0
httpx>=0.25.0

# Utilities
tqdm>=4.66.0
requests>=2.31.0

# Optional: Smith chart / RF tools (install separately if needed)
# scikit-rf>=0.30.0       # skrf for Smith chart, S-parameters
# plotly>=5.17.0          # Interactive 3D patterns in browser
# vtk>=9.2.0              # Full VTK library (heavy; legacy ASCII VTK works without this)
